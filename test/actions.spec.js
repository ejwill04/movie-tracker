import React from 'react';
// import { shallow } from 'enzyme';
import { expect } from './setup';
import * as actions from '../app/actions'

describe('actions', () => {
  describe('should create an action to add api movie data', () => {
    beforeEach(() => {
      // wrapper = shallow(<App />);
    });

    it.skip('blah', () => {
      expect(true).to.be.ok;
    });
  });


  describe('should create an action to add a favorite', () => {
    beforeEach(() => {
      // wrapper = shallow(<App />);
    });

    it.skip('blah', () => {
      expect(true).to.be.ok;
    });
  });


  describe('should create an action to add a user', () => {
    beforeEach(() => {
      // wrapper = shallow(<App />);
    });

    it.skip('blah', () => {
      expect(true).to.be.ok;
    });
  });
});
